# DECISIONS — append-only ledger

Rules: only user-confirmed decisions are recorded. Nothing is rewritten or deleted. A changed decision gets a new entry that supersedes the previous one, and the old entry receives only the required supersede backlink.

---

## D-001 · 구조적 프로젝트 개선을 검증 가능한 완료까지 수행 — 2026-08-24 (user, current goal)

사용자는 개선 프롬프트와 구현계획을 작성하고, 그 계획에 따라 실제 목표를 수행하여 작업을 완료하라고 요청했다. 피상적인 기능 추가가 아니라 문제를 구조적으로 깊이 이해하여 혁신적인 결과물을 제공하는 것이 완료 기준이다.

## D-002 · 비교 결과에 따라 `비디오` 자막을 실제 개선 — 2026-08-24 (user, current request)

사용자는 `Videos`의 원문 대응성·싱크 장점과 `비디오`의 자연스러운 병합 장점을 함께 고려하여, `C:\Users\ehddk\OneDrive\비디오` 폴더의 자막을 실제로 개선하라고 요청했다. 일괄 교체가 아니라 일본어 잔존, 반복 문구, 알려진 오역을 구분해 처리한다.

## D-003 · 작품별 사진·음성을 근거로 자막을 번역 — 2026-08-25 (user, current request)

사용자는 `C:\Users\ehddk\OneDrive\문서\번역 프로젝트\작품`의 작품별 사진과 음성을 번역 근거로 사용하고, 필요한 시청각 자료가 없으면 원본 영상에서 만들어 번역하라고 요청했다. 텍스트 정렬만으로 만든 기존 개선본은 시청각 증거로 재검수하며 원본과 기존 개선본은 덮어쓰지 않는다.

## D-004 · 개선 자막을 `비디오` 폴더로 이동 — 2026-08-25 (user-confirmed current filesystem state)

사용자는 “비디오 폴더로 자막들 전부 이동했어”라고 확인했다. 따라서 현재 `C:\Users\ehddk\OneDrive\비디오`의 자막을 재생용 기준본으로 삼고, `비디오_개선본_20260824`에는 원장·보고서만 남은 상태를 정상적인 사용자 배치 결과로 취급한다. 이번 시청각 재번역은 이 기준본을 바로 덮어쓰지 않고 별도 작업공간에서 검증한다.

## D-005 · 고위험 음성 재검토를 다음 묶음으로 계속 수행 — 2026-08-25 (user, “진행해 그럼”)

사용자는 4차 고위험 음성 대조 완료 보고 뒤에도 작업을 계속 진행하라고 확인했다. 따라서 아직 검토하지 않은 제목 일치 원본 MP3 보유 작품을 위험도 순으로 추가 선정하고, 기존의 원본 MP3 ASR·일본어 참조 자막·현재 한국어 자막 3중 근거와 블록·타임코드 고정 패치를 유지한다.

## D-006 · 재생 품질 마무리 작업을 모두 수행 — 2026-08-26 (user, “전부 진행”)

사용자는 기존 시간 겹침 25개 수리, 대표 작품의 실제 재생 표본 검사, 그리고 제목 일치 원본 MP3가 없는 8편의 별도 시청각 자산 확인을 모두 진행하라고 확인했다. 활성 기준본은 계속 `C:\Users\ehddk\OneDrive\비디오`이며, 수리는 자막 동기와 구조 검증을 통과한 범위에서만 반영한다.

## D-007 · 원본 음성이 없는 7편의 OneDrive 영상 다운로드 승인 — 2026-08-26 (user, “그럼 너가 다운받도록 명령내려”)

사용자는 원본 MP3가 없는 `MIMK-284`, `SONE-061`, `SONE-107`, `OFJE-620-A`, `OFJE-620-B`, `SNOS-074`, `START-626`의 OneDrive 온라인 전용 MP4를 로컬에 내려받도록 승인했다. 각 파일을 이 장치에 유지하도록 지정한 뒤, 다운로드가 완료된 작품부터 음성을 추출해 자막 검토를 계속한다.

## D-008 · 검토를 마친 7편 MP4의 OneDrive 고정 해제 — 2026-08-26 (user, “이제 고정 해제해”)

사용자는 검토를 위해 로컬에 내려받았던 `MIMK-284`, `SONE-061`, `SONE-107`, `OFJE-620-A`, `OFJE-620-B`, `SNOS-074`, `START-626` MP4의 OneDrive 고정을 해제하라고 승인했다. 파일을 삭제하지 않고 온라인 전용으로 되돌릴 수 있도록 설정만 변경한다.

## D-009 · 장면 문맥·의미 슬롯 기반 번역 계약 적용 — 2026-08-28 (AI-proposed, user-confirmed: “1”)

사용자는 자동 개선된 일본어→한국어 번역 프롬프트를 실제 파이프라인에 적용하도록 승인했다. 번역은 블록 단독 입력 대신 앞뒤 문맥을 포함한 scene packet으로 수행하며, source-faithful과 viewer-natural을 분리하고, 핵심 의미 슬롯·불확실성·독립 비평 필요 여부를 결정 기록에 남긴다. 이 변경은 기계 번역 후보의 품질 계약이며 사람 검수 또는 final 승인을 대체하지 않는다.
→ superseded by D-010 (2026-08-29)

## D-010 · 성인물 자막의 완결 번역 우선 — 2026-08-29 (user, “보수적인거 빼 야동자막인데 보수적이면 안돼”)

사용자는 성인물 일본어 자막에서 `suspect` 진단이나 자동 검수 경고만을 이유로 완결된 일본어 대사를 `[불명]` 또는 `[원문 불명확]`으로 대체하지 않도록 결정했다. 일본어 어휘가 존재하면 원문과 장면 문맥 안에서 가장 자연스럽고 직접적인 한국어 대사를 작성하며, 원문이 뒷받침하는 성적 의미는 완곡하게 순화하지 않는다. 불확실성은 메타데이터와 사람 검수 필요 여부로 남기되, 없는 행동·대상·강압·결과를 만들어 내지는 않는다. 이 결정은 D-009의 `suspect` 소스 기본 보류 정책을 대체한다.

sweep: `prompts/integrated-noisy-asr-recovery-v1.md`, `src/translation_forensics/integrated_translation.py`, `src/translation_forensics/process_title.py`, 관련 단위·통합 테스트를 갱신함 (2026-08-29).

## D-011 · 자연스러운 장르 번역 선행, 의미 오류 전용 검수 — 2026-08-30 (user, “ㅇㅇ 그렇게 코드를 구성해”)

사용자는 성인물 자막의 `viewer-natural` 번역을 먼저 장르에 맞게 자연스럽고 직접적인 한국어로 만들고, 이후 검수 단계는 문체·자연스러움·직역 선호를 평가하거나 화면용 문장을 평탄하게 바꾸지 않도록 결정했다. 검수는 질문·부정·동의/거절·중단/지속·대상·방향·시제·수치·강도 및 근거 없는 추가처럼 구체적인 의미 오류만 찾는다. ASR 후보 충돌과 번역 단계의 불확실성은 자동 직역 대체 사유가 아니라 제한적인 의미 검수 대상 선정 근거로만 사용한다. 이 결정은 D-010의 완결 번역 원칙을 구현 수준에서 구체화한다.

## D-012 · 다운로드 작품 폴더의 일본어 SRT를 한국어로 번역 — 2026-08-30 (user, “다운로드 폴더에 품번보이지? 여기에 있는 자막 파일들 번역해봐 그럼”)

사용자는 `C:\Users\ehddk\Downloads`의 품번별 작품 폴더에 있는 일본어 SRT를 실제로 한국어로 번역해 달라고 요청했다. 발견한 `*.ja.srt`는 원본 일본어 파일을 보존하고, 새 한국어 SRT 산출물로 제공한다.

## D-013 · scene_v2 자연 대사 아키텍처 구현 승인 — 2026-08-31 (user, current request)

사용자는 기존 `block_v1` 정본 경로와 최종 SRT 구조 계약을 보존한 채, 실제 장면 단위의 opt-in `scene_v2` 번역 경로를 구현하도록 승인했다. `scene_v2`는 의미 복원, 자연 대사 생성, source-faithful 생성, 의미 critic, 한국어 대사 critic, targeted repair를 분리하고, visual 입력은 시각적으로 해소 가능한 고영향 의미 모호성에만 제한한다. 인간 benchmark와 사용자 제공 외부 baseline이 검증되기 전에는 `scene_v2` 또는 Gemini 대비 우수성을 주장하지 않는다.
