# translation-forensics 작업 규칙

## 번역 요청 자동 라우팅

이 저장소에서 일본어 자막 번역·복원·검수 요청을 받으면 작업 전에 다음 문서를 읽고 적용한다.

1. `prompts/batch-adult-srt-translation-run.md` — 작품 단위 전체 작업 흐름과 중단 기준
2. `prompts/terra-semantic-translation-v1.md` — GPT-5.6 의미 번역 결정 형식
3. `docs/TRANSLATION_DECISIONS.md` — 결정 JSONL과 SRT 적용 계약

사용자가 긴 프롬프트를 다시 붙여 넣지 않아도 이 규칙을 기본값으로 사용한다. 먼저 파일·근거·시간축을 점검하고, 누락·충돌·미검증 상태를 보고한 뒤 번역을 시작한다. `final` 승격은 별도의 사람 검수와 평가 게이트가 충족될 때만 허용한다.

사용자가 보낼 수 있는 짧은 명령 예시는 다음과 같다.

```text
프로젝트 규칙에 따라 <작품번호> 번역을 시작해. 먼저 자료·근거·누락·충돌을 점검하고, 승인 없이 final을 만들지 마.
```

여러 작품 또는 작업 폴더의 작품 전체는 다음처럼 요청한다.

```text
지정한 작품들을 프로젝트 규칙에 따라 순차 처리해. 작품 수를 임의로 가정하지 말고, 각 작품마다 자료 점검→근거 연결→GPT-5.6 의미 번역→검증 순서로 진행하고, 보류 사유와 파일 경로를 기록해.
```

## 기본 원칙

- 기존 한국어 번역을 정답으로 취급하지 않는다.
- 먼저 일본어 발화의 실제 의미를 복원한 뒤 한국어 자연화를 별도로 수행한다.
- 블록 하나가 아니라 앞뒤 문맥과 장면 단위로 판단한다.
- 질문·대답, 부탁·거절, 명령·반응의 연결을 확인한다.
- 부정, 질문, 명령, 허용, 거절, 화자, 대상, 위치를 임의로 바꾸지 않는다.
- 확실한 의미와 불확실한 세부사항을 분리하고, 불확실한 부분을 창작으로 채우지 않는다.
- 성적 의미가 확인되면 불필요하게 순화하지 않되 원문보다 더 노골적인 내용은 추가하지 않는다.
- 화면에 보이는 사실과 실제 발화 내용을 구분한다.
- P1·P2만 처리하고 전체 작업이 끝났다고 보고하지 않는다.
- 수행하지 않은 검증을 통과했다고 보고하지 않는다.
- 작품별 결과를 공용 오류 기억·전역 규칙에 자동 편입하지 않는다.

## 콘텐츠 경계

입력은 성인 출연자의 성인용 영상과 자막으로 전제한다. 이 프로젝트는 번역·복원·검증만 수행하며, 원문에 없는 성적 창작이나 강도 상승을 하지 않는다.

## 증거 우선순위

1. 일본어 청취 가능자가 직접 확인한 명확한 원음과 영상
2. 문법·음가·문맥이 명확한 일본어 원문 또는 일본어 ASR
3. 로컬 다중 ASR의 공통 핵심과 독립적인 대체 일본어 ASR
4. 앞뒤 질문·대답과 장면 흐름
5. 타임스탬프 사진과 화면 정보
6. 기존 한국어 후보본

사진에 보인다는 이유만으로 발화에 없는 행동·위치·신체 부위·감정·관계를 자막에 추가하지 않는다.

## 하드 제약

별도 지시가 없으면 구조 기준본의 블록 수·번호·타임코드·순서를 변경하지 않는다.

금지 사항:

- 블록 추가·삭제·병합·분할
- 번호·타임코드·순서 변경
- 빈 최종 자막
- 최종 SRT의 일본어 잔존
- 번역자 주석, 판정 근거, 작업용 태그
- 원문에 없는 화자명·효과음·상황 설명

모든 최종 SRT는 UTF-8, LF로 저장한다. 구조 기준본 자체에 오류가 있으면 임의로 고치지 말고 QA 보고서에 기록한다.

## 변경 원칙

원본 ZIP, 입력 SRT, 음성, 사진, 기존 결과물은 덮어쓰지 않는다. `vendor/`는 제공된 구현체의 보존 영역이며, 수정이 필요하면 통합 어댑터에 우선 반영하고 원본 변경 이유와 테스트를 기록한다.
